@{
    // Esta parte define que todas las vistas de la aplicación, 
    // por defecto, van a usar el _layout automáticamente como
    // página maestra inyectadas en la parte de @RenderBody()
    Layout = "_Layout";
}
